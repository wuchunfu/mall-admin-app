<!--custom-tab-bar/index.wxml-->
<view>
  <van-tabbar active="{{ active }}" active-color="#1989FB"
  inactive-color="#000" bind:change="onChange">
    <van-tabbar-item wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}">{{
      item.text
    }}</van-tabbar-item>
  </van-tabbar>
</view>
